# Copy to .env.local and adjust. .env.local is gitignored.

# --- SQL Server ---
ACCEPT_EULA=Y
MSSQL_SA_PASSWORD=Dev_Str0ng!2024
MSSQL_PID=Developer
DB_NAME=CarApp

# --- Backend datasource (consumed by Spring Boot) ---
DB_HOST=car-db
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=Dev_Str0ng!2024

# --- JWT ---
JWT_SECRET=change-me-with-a-long-random-base64-secret-at-least-32-bytes
JWT_EXPIRATION_MS=3600000

# --- CORS ---
CORS_ALLOWED_ORIGINS=http://localhost:4200
