/**
 * Development defaults: the Angular dev server and the API live on different
 * ports, so the API needs an absolute origin.
 */
export const environment = {
  apiBaseUrl: 'http://localhost:8080',
};
